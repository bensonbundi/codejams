function factor(){

let mem = {}
 return function factorialize(n) {
  if (num < 0) 
        return -1;
  else if (num == 0) 
      return 1;
  else {
      if (n in mem) { return mem[n] )
      memo[n] = (num * factorialize(num - 1));
      return mem[n];
  }
}
}
}

